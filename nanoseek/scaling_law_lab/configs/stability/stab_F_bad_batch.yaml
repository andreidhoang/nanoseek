# ============================================================================
# Stability Ablation F — Bad Batch Injection (Recovery Dynamics)
# ============================================================================
# Injects a 10× gradient spike at step 1500 into an otherwise baseline run.
# Measures: how many steps to recover to pre-spike loss level?
#
# Pre-registered hypothesis:
#   Recovery within 50 steps.
#   grad_clip + bias reset provides sufficient robustness.
#
# Launch:
#   python -m nanoseek.scripts.pre_train \
#       --run "stab-F-bad-batch" \
#       --scale anchor \
#       --inject-bad-batch 1500 \
#       --seed 42
# ============================================================================

# Applied via CLI flag --inject-bad-batch 1500
