# ============================================================================
# Stability Ablation D — No Gradient Clipping
# ============================================================================
# Disables gradient clipping (max_norm=inf).
# All other V3.2 techniques remain active.
#
# Pre-registered hypothesis:
#   Without grad clip, loss spike occurs at step ~500-1000.
#   MoE gradient variance (1/κ = 8×) causes occasional large updates in bf16.
#
# Launch:
#   python -m nanoseek.scripts.pre_train \
#       --run "stab-D-no-gradclip" \
#       --scale anchor \
#       --no-grad-clip \
#       --seed 42
# ============================================================================

# Applied via CLI flag --no-grad-clip
# Equivalent to: max_grad_norm: inf
